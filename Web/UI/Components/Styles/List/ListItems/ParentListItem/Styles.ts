// Web/UI/Components/Styles/List/ListItems/ParentListItem/Styles.ts
import { createStyles, makeStyles, Theme } from '@material-ui/core/styles';

const drawerWidth = '240px';

export const useStyles = makeStyles((theme: Theme) =>
  createStyles({
    root: {
      paddingLeft: '32px',
    },
  }),
);
